# Corrected clean-story production update — 2026-06-26

## Branch and runtime state

Work continues on `codex/corrected-runtime-pipeline`. The separate download-directory
checkout is on local `main` with `origin/main [gone]` and is not the active production
worktree.

MuMu/ADB is reachable at `127.0.0.1:16384`; `pidof com.universal777.magireco` returned
PID `3213`. The forwarded Frida endpoint at `127.0.0.1:27043` still returns
`Failed to enumerate processes: connection closed`, so this pass does not introduce new
runtime captures. Static-evidence fixes are only applied where the event timeline is already
unambiguous.

## ac5208 partial clean-story recovery

`ac5208_001` and `ac5208_002` failed only because their static manifests contained normal
512x288 attack animation layers plus black-matte `ac8002_chance_btn` slot UI layers. Frame
contact-sheet inspection in
`A:\magireco_corrected_research_20260612\frame_audits_v18_next_candidates` confirms
`ac5208_lev_say` and `ac5208_lev_kyo` are normal attack animations, while
`ac8002_chance_btn` is button material. The CSV summary is
`ac5208_frame_audit_summary.csv`.

New composition plans:

- `tools/frida_runtime_probe/composition_plans/ac5208_001.json`
- `tools/frida_runtime_probe/composition_plans/ac5208_002.json`

Both plans keep only the main animation and LP loop at native 512x288:

- `ac5208_001`: `ac5208_lev_say` 0-4033 ms, `ac5208_lev_say_LP` from 4033 ms, render
  duration 6288 ms from official `40001` audio end `677+5611`.
- `ac5208_002`: `ac5208_lev_kyo` 0-4167 ms, `ac5208_lev_kyo_LP` from 4167 ms, render
  duration 6312 ms from official `40004` audio end `3371+2941`.

Rebuilding `production_manifests_v18` after the plans gives 926 events, 553 render-ready
events, 373 failed events, 646 composition-resolved events, 69 audience-excluded events,
and 1612 subtitles. The clip count drops from 2680 to 2676 because the two clean story
events no longer carry the four `ac8002` button-layer clips.

Single-event render and QA output:

```text
A:\magireco_corrected_research_20260612\validation_outputs_v18_clean_story_ac5208_partial
```

QA passed 2/2. Both subtitle and no-subtitle editions are 512x288, 30/1 fps, yuv420p,
48 kHz stereo, non-silent, and have identical audio essence per event. SRT cues:

- `ac5208_001`: `はぁぁっ` from 3233 to 4076 ms.
- `ac5208_002`: `せりゃー` from 3267 to 4703 ms.

`ac5208_003` is deliberately not fixed in this pass. Its static timeline starts
`ac5208_lev_madhom` at 3267 ms, while the contact sheet confirms the source clip itself is
a normal attack animation. Without runtime evidence or an explicit leading-gap decision,
shifting that clip to 0 ms risks incorrect sound/image sync. It remains failed with
`unresolved_video_composition` and `video_timeline_nonzero_start`.

No same-scene long edition is generated for `ac5208` yet; the family is incomplete and
should not be promoted to a Bilibili-oriented long video until `_003` is resolved.

## Project handoff kit

`reproducibility/project-kit/` now indexes the non-Python project assets and release/input
boundaries needed for collaborators:

- `repo-required-nonpython-files.csv` lists Frida JS probes, gadget config, composition
  plans, JSON overrides/exclusions, PowerShell scripts, docs, and release metadata that
  belong in Git.
- `large-input-release-plan.csv` records the APK/split APK/OBB/native/binary-table
  fingerprints and states that public GitHub Releases should carry only fingerprints and
  derived evidence, not proprietary payloads.
- `analysis-kit-layout.json` records the branch, public-release allowed extensions, and
  pipeline constraints: native resolution only, no upscale, no old 1080p batch, no deprecated
  motion/static classification, no suffix-to-CRI inference, and strict subtitle/no-subtitle
  separation.

The public release path remains text-only derived evidence bundles generated by
`reproducibility/scripts/New-AnalysisEvidenceBundle.ps1`.

## ac2201_001 clean-story exclusion

After v18.11, the next non-excluded failed event was `ac2201_001`. It is a native 416x232
`黒江狙え` slot/gameplay cut-in with eight overlapping layers: Kuroe character cut-in/LP,
ADD/MUL red effect layers, and `押し順/狙え` gameplay text borrowed from the `ac2101`
moji assets. A contact-sheet audit was written to:

```text
A:\magireco_corrected_research_20260612\frame_audits_v18_ac2201_001
```

The Kuroe base layer is visually a character animation, but the whole event is not a clean
story/dialogue clip. The gameplay text is not a subtitle and should not be composited into a
Bilibili clean-story animation. The official voice label
`26056_kuroe_黒チャ狙え_私の望みってなんだっ-` is also explicitly truncated, so no subtitle
is accepted until ASR/runtime evidence verifies the full line.

`ac2201_001` is therefore added to `audience_exclusions.json` as slot/gameplay material.
Rebuilding `production_manifests_v18` keeps 553 render-ready events and 373 failed events,
while increasing `audience_excluded_events` from 69 to 70. No new story render or material
edition is produced in this pass.

## ac3102 RB gameplay UI exclusion batch

The next major failed prefix was `ac3102` with 57 non-excluded unresolved compositions. A
sample audit of `ac3102_007` shows the pattern clearly: a full RB space/background layer,
overlaid character cut-in/LP layers, and visible button/icon/text gameplay UI. The sample
event has a valid official voice subtitle (`あら？` from request 6440), so the blocker is
visual classification/composition rather than subtitle recovery.

Contact-sheet evidence for the sample is stored at:

```text
A:\magireco_corrected_research_20260612\frame_audits_v18_ac3102_007
```

These events are not clean story/dialogue animations. They are RB slot battle/gameplay
presentations and should be retained as material/gameplay candidates until a separate
material collection policy is defined. The 57 non-ready `ac3102` events were added to
`audience_exclusions.json` with a shared RB gameplay-UI reason. Rebuilding
`production_manifests_v18` keeps 553 render-ready events and 373 failed events while raising
`audience_excluded_events` from 70 to 127. No ac3102 render or long edition is produced.

## ac3103 RB roulette gameplay UI exclusion batch

`ac3103` was the next unresolved RB prefix. Sample `ac3103_001` contains a roulette/high-speed
RB background plus adjustment-shop text/icon overlays, character silhouettes, and effect
loops. It has a valid official subtitle (`いくわよぉ`, request 6447), but the visual content is
still gameplay UI rather than a clean story/dialogue animation.

Contact-sheet evidence for the sample is stored at:

```text
A:\magireco_corrected_research_20260612\frame_audits_v18_ac3103_001
```

The 24 non-ready `ac3103` events were added to `audience_exclusions.json` as RB
roulette/slot gameplay material. Rebuilding `production_manifests_v18` keeps 553 render-ready
events and 373 failed events while raising `audience_excluded_events` from 127 to 151.
No ac3103 render or long edition is produced.

## ac5102_003 clean attack animation recovery

`ac5102_003` was reviewed after the RB gameplay batches. The static production manifest
already provided a coherent native 416x232 attack-animation timeline:

- `ac5102_3on_01_S_c002b_MR` starts at 0 ms;
- `ac5102_3on_01_S_c002b_MR_lp` starts at 400 ms;
- `ac5102_3on_01_uwa_ef_bg_lp` starts at 467 ms and runs to the 1300 ms visual end.

Contact-sheet and rendered-output audits show Iroha's magiatack character animation with a
pink impact/effect background. No button, `押し順`, `狙え`, or slot UI text layer is present,
so this event is not treated like the ac2201/ac3102/ac3103 gameplay material exclusions.

New composition plan:

```text
tools/frida_runtime_probe/composition_plans/ac5102_003.json
```

Rebuilding `production_manifests_v18` after the plan gives 926 events, 554 render-ready
events, 372 failed events, 647 composition-resolved events, 151 audience-excluded events,
and 1612 subtitles.

Single-event render and QA output:

```text
A:\magireco_corrected_research_20260612\validation_outputs_v18_clean_story_ac5102_003_candidate
```

QA passed 1/1. Both subtitle and no-subtitle editions are 416x232, 30/1 fps, yuv420p,
48 kHz stereo, non-silent, and have identical audio essence. The official subtitle cue is
`やっ` from 33 to 533 ms. Rendered contact sheets are stored at:

```text
A:\magireco_corrected_research_20260612\frame_audits_v18_ac5102_003_render
```

The source-layer audit summary is:

```text
A:\magireco_corrected_research_20260612\frame_audits_v18_ac5102_003\ac5102_003_frame_audit_summary.csv
```
