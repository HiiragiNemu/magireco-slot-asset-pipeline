# Audience long-form authority reset — 2026-08-19

Production is paused while legacy audience compositions are re-audited. No
source media or immutable review-hub release is deleted, moved, or re-encoded.

## Binding product rule

1. Native 416x232 audience products have higher production priority than
   512x288. A 512x288 candidate is recorded and deferred until the
   evidence-ready 416x232 inventory is exhausted.
2. The default audience deliverable is a family/route long-form product. A
   single event archive is provenance/index material, not a human-review long
   film.
3. Separate route videos are generated only after an exact owner playback
   decision explicitly asks for them. Mutually exclusive outcomes in a long
   film are named chapters with shared events deduplicated. Such an edited
   compilation never claims to be one native session.
4. Silent visual material remains a language-neutral material archive.

## Immediate corrections

- `ac1103_013` is an 18-second terminal event archive. DirInfo kind 55 row 30
  is `ac1103_012 -> ac1103_013`; it is not a full ac1103 chapter.
- `ac7205_008` participates in 16 longer DirInfo routes and `ac7205_016` in 8.
  Their 6.167/9.567-second archives are source segments, not audience long
  films.
- `ac7002_001` is an independent DirInfo kind 177 row. The five ac7002 rows are
  not one native route; they may be assembled only as an explicitly edited,
  chaptered ac7002 family compilation.
- Legacy ac1102 `R0052` linearly concatenates mutually exclusive nodes. Its
  historical playback result remains recorded, but its authoritative
  composition claim is withdrawn.
- v77 has 16 evidence-bound route archives and preserves valid route
  relationships, but their union contains only 10 unique events. Legacy R0052
  contains 11 unique events because it also includes `_012`, while neither
  product closes the full 31-row family (and R0052 omits the paired `_013`).
  v77 is therefore better route evidence, not a more complete long film. Its
  files are inputs for a future deduplicated, chaptered ac1102 long film, not
  16 default audience products.

All remaining ac1102 DirInfo rows must be closed before the replacement is
called exhaustive or authoritative.
